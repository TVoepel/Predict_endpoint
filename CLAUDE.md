# Hinweise für Claude Code

- Nach jeder Neuerung, die gepusht wurde, am Ende der Antwort den GitHub-Download-Link
  des Branches als ZIP angeben, z. B.
  `https://github.com/TVoepel/Predict_endpoint/archive/refs/heads/<branch>.zip`,
  dazu die Terminal-Befehle zum Aktualisieren und Bauen (`./build_app.sh`).
- Sprache der Antworten und Oberfläche: Deutsch.
- Tests: `python3 -m unittest discover -s tests`.
